#include "stdafx.h"
#include <string>
#include <iostream>
using namespace System;
bool Guess(int number) {
	return false;
}
int main(array<System::String ^> ^args)
{
	int i;
	std::cin >> i;
	if (Guess(i) == true)
		std::cout << "YES";
	else
		std::cout << "NO";
}